# Conditions

## Vanilla
```ruby
accumulate(player, qmod, n, resource_type)
bring(player, unit, n, location)
command(player, unit, qmod, n, location)
commandTheLeast(unit)
commandTheLeastAt(unit, location)
commandTheMost(unit)
commandTheMostAt(unit, location)
countdownTimer(qmod, seconds)
deaths(player, qmod, n, unit)
elapsed(qmod, seconds)
highestScore(score_type)
killsOf(player, unit, qmod, n)
leastKills(unit)
leastResources(unit)
lowestScore(score_type)
mostKills(unit)
mostResources(unit)
never()
opponents(player, qmod, n)
switchIsState(id, state)
score(player, score_type, qmod, n)

```

## Custom
```ruby
memory(player_number, qmod, n)
```
