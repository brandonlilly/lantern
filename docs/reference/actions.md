# Actions

## Vanilla
```ruby
centerView(location)
createUnit(player, unit, n, location)
createUnitWithProperties(player, unit, n, location, properties)
comment(text)
defeat()
display(text)
draw()
give(player, unit, n, new_owner, location)
killUnit(player, unit)
killUnitAtLocation(player, unit, n, location)
leaderboardPoints(label, score_type)
leaderboardComputers(state)
leaderboardControl(label, unit)
leaderboardControlAtLocation(label, unit, location)
leaderboardGreed(amount)
leaderboardKills(label, unit)
leaderboardResources(label, resource_type)
leaderboardGoalPoints(label, score_type, amount)
leaderboardGoalControl(label, unit, amount)
leaderboardGoalControlAtLocation(label, unit, location, amount)
leaderboardGoalKills(label, unit, amount)
leaderboardGoalResources(label, resource_type, amount)
ping(location)
modifyHealth(player, unit, n, location, percent)
modifyHangar(player, unit, n, location, amount_to_add)
modifyEnergy(player, unit, n, location, percent)
modifyResource(player, num_sources, amount, location)
modifyShield(player, unit, n, location, percent)
moveLocation(move_loc, player, unit, dest_loc)
moveUnit(player, unit, n, at_loc, to_loc)
mute()
order(player, unit, at_loc, order, to_loc)
pauseGame()
pauseTimer()
preserveTrigger()
removeUnit(player, unit)
removeUnitAtLocation(player, unit, n, location)
runAIScript(script)
runAIScriptLocation(script, location)
setAlliance(player, ally_status)
setDoodadState(player, unit, location, state)
setCountdownTimer(vmod, seconds)
setDeaths(player, vmod, n, unit)
setInvincibility(player, unit, location, state)
setMissionObjectives(text)
setResources(player, vmod, n, resource_type)
setScore(player, vmod, n, score_type)
setSwitch(switch_id, value)
talkingPortrait(unit, ms)
transmission(text, unit, location, wav_path)
unmute()
unpauseGame()
unpauseTimer()
victory()
wait(ms)
```


## Custom
```ruby
```
